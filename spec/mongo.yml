test:
  clients:
    default:
      database: document_catalog_test
      hosts:
        - localhost:27017
      options:
        read:
          mode: :primary
        max_pool_size: 1
        connect_timeout: 1